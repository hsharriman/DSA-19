
public class LargestSubArray {
    static int[] largestSubarray(int[] nums) {
        /*turn all 0's into -1's, keep track of running sum.
        initialize array2[] to be len of nums, to compare fragments to running sum
         */
        return new int[]{0, 0};
    }
}
